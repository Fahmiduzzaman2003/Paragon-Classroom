# ─────────────────────────────────────────────────────
# Production dependencies (Render Free). Lean on purpose:
# NO chromadb / onnxruntime / torch — prod uses pgvector + hosted Gemini
# embeddings, so nothing loads a local ML model (that would OOM 512 MB).
# Local dev that wants the zero-key ONNX path: pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
gunicorn>=22.0
sqlalchemy[asyncio]>=2.0.35
aiosqlite>=0.20
asyncpg>=0.29
psycopg[binary]>=3.2          # sync driver for the pgvector store
pgvector>=0.3                 # (optional helpers; SQL uses text casts)
alembic>=1.13
pydantic>=2.8
pydantic-settings>=2.5
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
python-multipart>=0.0.12
bcrypt==4.0.1
httpx>=0.27
python-dotenv>=1.0
rank-bm25>=0.2.2
pypdf>=5.0
python-docx>=1.1
python-pptx>=1.0
openai>=1.50                  # also drives Groq (OpenAI-compatible endpoint)
anthropic>=0.40
google-generativeai>=0.8      # Gemini LLM + embeddings
email-validator>=2.2
slowapi>=0.1.9
aiosmtplib>=3.0
authlib>=1.3
sentry-sdk[fastapi]>=2.0      # error tracking (no-op unless SENTRY_DSN set)
